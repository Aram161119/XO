export const selectWinner = (state) => state.winner;
export const selectCurrentPlayer = (state) => state.currentPlayer;
export const selectIsDraw = (state) => state.isDraw;
export const selectBoard = (state) => state.board;
export const selectWinnerLine = (state) => state.winnerLine;
